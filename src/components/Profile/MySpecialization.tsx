import { CenterBox } from "../common/Box";
import { Specialization } from "./index.styled";

const MySpecialization = () => {
  return (
    <CenterBox py="0.75rem">
      <Specialization />
    </CenterBox>
  );
};

export default MySpecialization;
